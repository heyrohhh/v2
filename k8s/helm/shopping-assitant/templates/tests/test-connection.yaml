apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopping-assitant.fullname" . }}-test-connection"
  labels:
    {{- include "shopping-assitant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopping-assitant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
