apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopping-assitant.fullname" . }}
  labels:
    {{- include "shopping-assitant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopping-assitant.selectorLabels" . | nindent 4 }}
