apiVersion: v1
kind: Service
metadata:
  name: {{ include "load-genrator.fullname" . }}
  labels:
    {{- include "load-genrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "load-genrator.selectorLabels" . | nindent 4 }}
